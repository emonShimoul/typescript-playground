"use strict";
;
;
const users = {
    name: 'Omanush',
    age: 2000,
    role: 'Unknown'
};
const addNumbers = (num1, num2) => num1 + num2;
const rollnumbers = [1, 4, 5]; // [index]
// NOTE: It is better use type alias with function and array. Interface should should be used only with object and class.
